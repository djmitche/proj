code
----
* tests
  * travis
* parse ~/.projrc.yml, too
* use internal packages

child types
-----------
* docker run

features
--------
* child alias (prepend to path)
* default child (so e.g., `bb` can open `bb/buildbot` by default, but
  `bb/infra` goes to infra)
* specify subdir when ssh'ing (so can use p/)
* run the last `.projrc` detected? or just always run ~/.projrc shell script?
  or get that from ~/.projrc.yml?
* better config format that can be edited by Ansible -- .proj-<child>.yml?
* default to 'cd' when no child config is found

modifiers (these will be in shell code)
---------------------------------------
* npm environment?
* ruby environment?
* enable git prompt
* enable hg prompt
* append to PATH
* raw shell input
* gopath (just take a relative path)
* vimrc modifications

organization
------------

Refactor as a glorified "cd":
 - can span hosts / containers
 - can specify a bashrc (so the rest is up to you)

So, needs to be paired with:
 - dotfiles, for shell scripts
 - Ansible, for host configuration
