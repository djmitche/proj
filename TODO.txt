code
----
* tests
  * travis
* parse ~/.projrc.yml, too
* shell quoting

child types
-----------
* docker run
* ec2 instance (start automatically)
* alias (prepend to path)

modifiers
---------
* npm environment?
* ruby environment?
* enable git prompt
* enable hg prompt
* append to PATH
* raw shell input
* gopath (just take a relative path)
* vimrc modifications

features
--------
* interpolate path into env var (for prompt)
